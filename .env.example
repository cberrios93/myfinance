# Supabase — proyecto DEV
# Supabase → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase — service role (solo para Edge Functions, nunca exponer al frontend)
# Supabase → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — para OCR de boletas PDF
# console.anthropic.com → API Keys
ANTHROPIC_API_KEY=
